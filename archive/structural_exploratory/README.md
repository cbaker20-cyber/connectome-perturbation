# Exploratory structural support

This file contains one early null-aggregation helper used during intermediate
analysis. It is retained only as development history and is not a current
experiment entry point. The code predates the corrected empirical p-value
implementation; use the current null runners and statistics code for new
results.
